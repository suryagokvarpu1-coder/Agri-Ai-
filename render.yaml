services:
  - type: web
    name: agri-ai
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node simple-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
